<form method="post" action="{{ path('app_admin_renting_delete', {'id': renting.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer définitivement cette commande ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ renting.id) }}">
    <button class="btn btn-danger"><i class="fa-solid fa-trash-can"></i></button>
</form>
