<nav class="navbar fixed-top navbar-expand-lg bg-dark navbar-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">AGENCELOC</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="{{ path('app_front_home_index') }}">Accueil</a>
        </li>
        {% if is_granted('ROLE_USER') %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('app_front_user_account') }}">Mon compte</a>
          </li>
          {% if is_granted('ROLE_ADMIN') %}
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                <i class="fa-solid fa-gears"></i> BackOffice
              </a>
              <ul class="dropdown-menu">
                <li><a class="dropdown-item" href="{{ path('app_admin_user_index') }}"><i class="fa-solid fa-user-group"></i> Membres</a></li>
                <li><a class="dropdown-item" href="{{ path('app_admin_vehicle_index') }}"><i class="fa-solid fa-car"></i> Véhicules</a></li>
                <li><a class="dropdown-item" href="{{ path('app_admin_renting_index') }}"><i class="fa-solid fa-money-check"></i> Commandes</a></li>
              </ul>
            </li>
          {% endif %}
        {% else %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('app_front_user_signup') }}">Inscription</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{ path('app_front_user_login') }}">Connexion</a>
          </li>
        {% endif %}
      </ul>
    </div>
  </div>
</nav>